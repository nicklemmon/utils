# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) to version packages and write changelogs.

```bash
npm run changeset
npm run version-packages
```

Publishing to npm is not automated yet.
