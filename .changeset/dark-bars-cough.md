---
"@nicklemmon/gh-gl": minor
---

Add the `gh-gl` CLI. It syncs a GitHub repo's default branch into a downstream GitLab repo, applies an overlay for GitLab-only files, and can merge that result into prototype branches.
