# @nicklemmon/gh-gl

## 0.1.0

### Minor Changes

- 331f3e1: Add the `gh-gl` CLI. It syncs a GitHub repo's default branch into a downstream GitLab repo, applies an overlay for GitLab-only files, and can merge that result into prototype branches.
